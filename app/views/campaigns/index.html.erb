<div class="content">
  <div class="row">
    <div class="col-md-6 col-md-offset-3">
      <h1>campaigns</h1>

      <ul>
        <% @campaigns.each do |campaign| %>
          <li><%= link_to campaign.title, campaign %></li>
        <% end %>
      </ul>

    </div> 
  </div>
</div>